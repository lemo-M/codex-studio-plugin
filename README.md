# Codex Selection Toolbar (Android Studio Plugin)

Select code in editor and click **Send to Codex** on floating toolbar. The action copies:

- `@relative/path/File.kt#Lstart-end`
- selected code text

## Run

```bash
./gradlew runIde
```

## Build

```bash
./gradlew buildPlugin
```
